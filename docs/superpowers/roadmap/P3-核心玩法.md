# P3 核心玩法

> 对应总设计: `../specs/2026-06-18-novel-interactive-story-simulator-design.md`
> 预估工时: ~3 周
> 验收标准: 用户可以选择作品、探索节点地图、做出选择/转盘抽奖、LLM 生成故事、存档读档完整跑通

---

## 本阶段目标

完成 Player 端的核心游戏体验，包括作品选择、节点地图探索、选项面板、转盘抽奖、LLM 事件和故事生成、SSE 流式输出以及存档功能。

---

## 进度

| 子阶段 | 状态 | 说明 |
|--------|------|------|
| P3-A Player 基础 | ✅ 已完成 | 作品列表、会话管理、角色创建、故事主界面骨架 |
| P3-B 核心玩法循环 | ✅ 已完成 | ActionEngine、EventChain(stub)、StoryChain(stub)、SSE流式、转盘、存档、结局 |
| P3-C LLM 深度参与 | ✅ 已完成 | StoryChain/EventChain LLM 调用，OptionChain LLM 生成选项 |
| P3-D 完善打磨 | ✅ 已完成 | 死亡判定、移动端适配 |
| **玩法流程重构** | ✅ **已完成** | 因果链系统：选择→检定→机械结果→叙事，d20 检定公式，统一 resolve API |

---

## 已完成任务清单

### 3.1 后端：Player 作品列表 ✅
- [x] `GET /api/player/novel/list` — 根据用户角色列表查询可见作品
- [x] `GET /api/player/novel/{novelId}/full` — 完整节点树
- [x] `GET /api/player/node/{nodeId}` — 节点详情

### 3.2 后端：游戏会话（存档） ✅
- [x] `POST /api/player/session/create` — 创建会话
- [x] `GET /api/player/session/{id}` — 获取存档状态
- [x] `POST /api/player/session/save/load/list/restart` — 完整存档体系

### 3.3 后端：玩法流程重构（因果链系统） ✅
- [x] `POST /api/player/action/resolve` — **统一端点**，替代旧 choose+spin
- [x] safe/risky/daring 三级风险区分
- [x] d20 属性检定公式（属性值参与修正）
- [x] 关联属性自动识别（从选项 label 关键词推断）
- [x] EventChain 接收 riskLevel，luck 影响事件偏向
- [x] StoryChain 接收 ResolutionResult，检定数据嵌入 prompt
- [x] ResolutionResult DTO（替代旧 ActionResult）
- [x] OptionVO 新增 riskLevel/attrHint/expectedOutcome 字段
- [x] OptionChain prompt 扩展，LLM 生成带风险标签的选项

### 3.4 后端：故事生成 + SSE 流式 ✅
- [x] SSE 流式输出（SseEmitter）
- [x] StoryChain — 基于节点、属性、事件描述生成上下文故事
- [x] StoryChain — 接收 ResolutionResult 重载
- [x] StoryChain.generateEnding() — 结局总结
- [x] 全量 OpenAI chat 对话历史（Redis）

### 3.5 前端：作品选择/设置页 ✅
- [x] 卡片网格展示可见作品，游客/登录用户差异化
- [x] 角色名称 + 属性抽奖转盘
- [x] 骨架屏 + 空状态

### 3.6 前端：故事主界面（玩法重构后） ✅
- [x] ChoicePanel — 带风险标签（安全/冒险/高危），attrHint + expectedOutcome
- [x] **ResolutionDisplay** — 检定结果展示（skill check 表格/轮盘/事件卡片/属性变化）
- [x] WheelOfFortune — 自动播放动画，扇区由后端决定
- [x] StoryViewer — 字符位置分界，CSS hidden 保留滚动位置，"📖 故事继续"标记
- [x] CharacterPanel — 属性变化浮动数字动画
- [x] 全局 resolve 加载指示器
- [x] useStory — resolveAction 替代 chooseAction+spinAction
- [x] EndingModal/SaveLoadModal

---

## 关键设计文档

| 文档 | 内容 |
|------|------|
| `specs/2026-06-26-gameplay-flow-redesign.md` | **玩法流程重构设计** — 因果链系统完整设计 |
| `plans/2026-06-26-gameplay-flow-redesign.md` | **实施计划** — 16个 task 分步实施 |
| `gameplay/gameplay-logic.md` (v2.0) | **流程定义** — 完整游戏循环、数据流、检定公式 |
| `gameplay/llm-deep-participation.md` | **LLM 方案** — Chain 设计、Prompt |
| `gameplay/gameplay-mechanics-design.md` | **玩法机制** — 属性驱动、骨架血肉分离 |

---

## 参考

- 总设计 §6 (Chain设计), §7 (玩家交互), §8.2 (Player API)
